import { Rectangle } from "components/Rectangle";

export default {
    title: "Components/Rectangle",
    component: Rectangle,
  };
  
  const Template = () => <Rectangle/>

  export const primary = Template.bind({});