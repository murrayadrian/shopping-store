import { SliderProducts } from "components/Slider";

export default {
    title: "Components/Sliders",
    component: SliderProducts,
  };
  
  
  const Template = () => (
    <SliderProducts />
  )

  export const bestseller = Template.bind({});